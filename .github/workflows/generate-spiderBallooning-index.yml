name: Generate spiderBallooning index.html

on:
  push:
    branches: [ "main" ]
    paths:
      - "spiderBallooning/**"
      - "scripts/generate-spiderBallooning-index.mjs"
      - ".github/workflows/generate-spiderBallooning-index.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate spiderBallooning/index.html
        run: node scripts/generate-spiderBallooning-index.mjs

      - name: Commit changes (if any)
        run: |
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add spiderBallooning/index.html
          git commit -m "Auto-generate spiderBallooning/index.html"
          git push

